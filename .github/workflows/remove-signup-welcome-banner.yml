name: Remove signup welcome banner
on:
  push:
    paths:
      - '.github/workflows/remove-signup-welcome-banner.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node .github/remove-signup-welcome-banner.cjs
      - run: node --check _worker.js
      - name: Commit update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add _worker.js .github
          git commit -m "Remove welcome banner from signup page"
          git push
