name: Add member welcome banner

on:
  push:
    paths:
      - .github/add-member-welcome-banner.cjs
      - .github/workflows/add-member-welcome-banner.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node .github/add-member-welcome-banner.cjs
      - run: node --check _worker.js
      - run: |
          git config user.name "bare-dot-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Add welcome banner to member pages"
          git push
